ViewTrafficType:
  required:
    - name
    - value
  properties:
    name:
      $ref: '#/components/schemas/TrafficField'
    value:
      type: string
      description: 'Translation of the value depending on the Accept-Language header'
      example: 'large'